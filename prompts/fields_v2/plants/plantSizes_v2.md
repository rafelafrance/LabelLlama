---
name: plantSizes
description: Extract dimensions of individual plant parts and structures, excluding the overall plant height (which belongs in `plantHeight`)
module: llama/fields/plants/plantSizes.py
---

# Prompt plantSizes

`plantSizes` (list[str]): Extract dimensions of individual plant parts and structures, excluding the overall plant height (which belongs in `plantHeight`)
